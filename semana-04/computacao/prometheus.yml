global:
  scrape_interval: 5s  # Coletar métricas a cada 5 segundos

scrape_configs:
  - job_name: "python_app"
    static_configs:
      - targets: ["host.docker.internal:8000"]  # Se estiver rodando no Docker
